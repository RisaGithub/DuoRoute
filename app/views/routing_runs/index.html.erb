<%= render "shared/page_head", eyebrow: "Replay & history", title: "Запуски", subtitle: "Каждый run хранит входы, конфигурацию, seed и полный audit trail.", action_label: "Новый запуск", action_path: new_run_path %>
<%= render "shared/run_table", runs: @runs %>
