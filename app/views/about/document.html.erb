<% content_for :title, "#{@title} · DuoRoute" %>
<%= render "shared/page_head", eyebrow: "Документация проекта", title: @title, subtitle: "Актуальная документация из репозитория проекта." %>
<div><%= link_to "← О продукте", about_path(anchor: "documentation"), class: "btn" %></div>
<article class="product-doc"><%= render_document(@source) %></article>
