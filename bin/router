#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ruby_preflight"
RubyPreflight.check!

$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
require "duo_route"

exit DuoRoute::CLI::App.new(ARGV).run
